[pytest]
pythonpath = .
markers =
    unit: fast tests for one module or layer with external services mocked
    integration: tests that exercise multiple backend layers together
    api: FastAPI endpoint tests using TestClient
    scheduler: scheduler tests
    worker: worker execution tests
    auth: authentication and authorization tests
